---
import type { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'a'> {
	primary?: boolean;
	secondary?: boolean;
}

const {
	href = '#',
	target = '_blank',
	rel = 'noreferrer',
	class: className,
	primary,
	secondary,
	...attrs
} = Astro.props;

const classes = [
	'link',
	{
		'link-primary': primary,
		'link-secondary': secondary,
		[className ?? '']: className,
	},
];
---

<a href={href} target={target} rel={rel} class:list={classes} {...attrs}>
	<slot />
</a>
